# Copy to `.env` in this folder (`cp .env.example .env`) and set values locally.
# Never commit `.env` or paste real API keys into `.env.example` — example files are often checked into git.
#
# Create a key: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional overrides
# EMBEDDING_MODEL=text-embedding-3-small
# TOP_K=10
