# Leave unset to use the Vite dev proxy: browser calls `/api/*` → http://127.0.0.1:8000
#
# To call the API directly (no proxy), set the FastAPI origin only — no `/api` path:
# VITE_API_BASE=http://127.0.0.1:8000
